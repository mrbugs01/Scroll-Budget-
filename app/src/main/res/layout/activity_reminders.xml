<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_app_gradient"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="⏰ Reminders"
            android:textColor="@color/brand_text_primary"
            android:textSize="24sp"
            android:textStyle="bold"
            android:layout_marginBottom="4dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Your phone will vibrate strongly when a reminder fires."
            android:textColor="@color/brand_text_secondary"
            android:textSize="13sp"
            android:layout_marginBottom="20dp" />

        <!-- Add new reminder -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="@drawable/bg_card_premium"
            android:layout_marginBottom="20dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Add a reminder"
                android:textColor="@color/brand_text_primary"
                android:textSize="15sp"
                android:textStyle="bold"
                android:layout_marginBottom="12dp" />

            <EditText
                android:id="@+id/reminderLabelInput"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="What's this reminder for? (e.g. Study time)"
                android:textColor="@color/brand_text_primary"
                android:textColorHint="@color/brand_text_secondary"
                android:textSize="13sp"
                android:background="@drawable/bg_button_secondary"
                android:layout_marginBottom="12dp" />

            <Button
                android:id="@+id/btnPickTime"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@drawable/bg_button_secondary"
                android:text="🕐  Pick time"
                android:textColor="@color/brand_text_primary"
                android:textAllCaps="false"
                android:elevation="0dp"
                android:layout_marginBottom="12dp" />

            <Button
                android:id="@+id/btnAddReminder"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@drawable/bg_button_primary"
                android:text="Add Reminder"
                android:textColor="@color/brand_text_primary"
                android:textAllCaps="false"
                android:elevation="0dp" />

        </LinearLayout>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Your reminders"
            android:textColor="@color/brand_text_primary"
            android:textSize="15sp"
            android:textStyle="bold"
            android:layout_marginBottom="12dp" />

        <LinearLayout
            android:id="@+id/remindersContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" />

        <TextView
            android:id="@+id/emptyStateText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="No reminders yet — add one above."
            android:textColor="@color/brand_text_secondary"
            android:textSize="13sp"
            android:gravity="center"
            android:layout_marginTop="12dp" />

        <Button
            android:id="@+id/btnBack"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_button_secondary"
            android:text="← Back"
            android:textColor="@color/brand_text_primary"
            android:textAllCaps="false"
            android:elevation="0dp"
            android:layout_marginTop="20dp" />

    </LinearLayout>
</ScrollView>
